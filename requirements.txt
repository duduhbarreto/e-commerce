# requirements.txt - Sistema E-commerce TAC-3
# Professor: Walter Felipe

# ============= BIBLIOTECAS ESSENCIAIS =============
pytest>=7.0.0
pytest-cov>=4.0.0

# ============= BIBLIOTECAS OPCIONAIS (para relatórios avançados) =============
# Descomente se quiser recursos extras:
# pytest-xdist>=3.0.0          # Para execução paralela
# pytest-mock>=3.10.0          # Extensões mock (não obrigatório)
# colorama>=0.4.6              # Para cores no terminal

# ============= OBSERVAÇÕES =============
# Bibliotecas já incluídas no Python (não precisam instalar):
# - unittest (biblioteca padrão)
# - threading (biblioteca padrão) 
# - concurrent.futures (biblioteca padrão)
# - time, uuid, datetime, enum (bibliotecas padrão)